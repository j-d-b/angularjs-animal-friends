import styles from './styles.css';
import template from './template.html';
import controller from './controller';

const bindings = {
  animals: '='
};

export default {
  bindings,
  controller,
  template
};
